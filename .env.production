# Loaded by `vite build` (production).
# Leave VITE_SPOTIFY_REDIRECT_URI unset so spotifyAuth.ts falls back to
# `${window.location.origin}/callback`, which works for the deployed SWA
# hostname (and any future custom domain) without rebuilding.
VITE_SPOTIFY_REDIRECT_URI=
