# Loaded by `vite` in development mode (npm run dev / dev:swa).
# Pin the redirect URI to the loopback origin Spotify is configured for locally.
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/callback
